# outputs (generated reports, not part of the skill)
*.html
*.xlsx
*.jpg
*.png

# but keep the bundled baseline sample report (visual reference for the skill)
!references/sample_*.html

# temp
*.tmp
Thumbs.db
.DS_Store
